# Rules: AI UX

1. **Loading:** Show a spinner in the modal when `loading` is true.
2. **Security:** The button must be disabled while loading.
3. **Apply:** Clicking "Apply" triggers the parent `onChange` with the result text.