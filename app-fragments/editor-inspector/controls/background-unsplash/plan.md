# Development Plan

1. **Data:** Define `MOCK_IMAGES` array.
2. **State:** `selectedCategory`, `searchQuery`.
3. **Components:**
   - Header (Search + Chips).
   - Grid (Images).
4. **Logic:** `renderImages()`, `handleSelect()`.