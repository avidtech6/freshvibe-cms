# Trace Atlas: Slider
- **Interaction**: `mousedown` on handle -> `mousemove` on document -> `mouseup` on document.
- **Editing**: `dblclick` on input -> `focus` on input -> `blur` on input.
- **State**: `currentValue`, `min`, `max`, `step`.