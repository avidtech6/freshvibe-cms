# Plan: Responsive Module
1. Define 3 rows.
2. Assign device types and IDs.
3. Implement `toggleOverride(key)` function.
4. Render logic: Check `override` state to decide enabled/disabled and input value.