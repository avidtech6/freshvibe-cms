# Trace Atlas: Dimension
- **State**: `value` (obj), `isLinked` (bool), `manualOverride` (obj, store last unlinked value).
- **Events**: `input` on fields, `click` on link icon.
- **Propagation**: When linked, inputs share event listeners effectively.