# Plan: Dimension Module
1. Create wrapper with Flexbox.
2. Generate 4 input slots.
3. Add Link Button (Icon + Label).
4. Implement `updateAll` function.
5. Implement `toggleLink` function.
6. Bind `input` events to main handler.