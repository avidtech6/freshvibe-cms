# Plan: Development

1. **HTML**: `div.fes-control-number > input + button(-) + button(+)`.
2. **CSS**: Flexbox. Input `flex-grow: 1`.
3. **JS**:
   - Default step = 1.
   - Clamp value helper.
   - Click handler for +/-.
   - Shift check.
   - Update input value and call onChange.