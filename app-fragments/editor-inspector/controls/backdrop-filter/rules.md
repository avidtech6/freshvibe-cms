# Rules: Backdrop UX

1. **Context:** The inspector typically edits a backdrop layer (e.g., a modal or card).
2. **Visuals:** Preview box should have a semi-transparent background color so `backdrop-filter` is visible (e.g., white with 50% opacity).
3. **Validation:** Ensure only valid CSS filter values are accepted.