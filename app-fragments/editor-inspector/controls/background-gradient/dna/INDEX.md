# Core Concepts

- **CSS Syntax Generation:** Converting JS object `{ type, angle, stops }` to `linear-gradient(...)`.
- **Relative Positioning:** Mapping 0-100 slider values to visual pixels on the preview bar.
- **Event Delegation:** Managing global mouse events for dragging.