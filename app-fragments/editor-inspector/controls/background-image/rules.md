# Rules: Image Constraints

1. **Fit Options:** `cover`, `contain`, `fill`, `none`.
2. **Blend Modes:** Standard CSS `mix-blend-mode` values.
3. **Overlay:** Only valid when image is present. Defaults to black with alpha.
4. **Empty State:** If URL is cleared, properties should be reset or disabled.