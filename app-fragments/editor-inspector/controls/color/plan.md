# Development Plan
1. Parse `field.value` to determine if alpha exists.
2. Build Flex container.
3. Create Swatch div.
4. Create Input `type="text"`.
5. If `showAlpha`, create `type="range"`.
6. Wire up listeners.